//=============================================== 1//Write a function called percentOf that takes two parameters,//The function should find out what percentage the first number represents of the second number, //and returns the result as a string.//percentOf(5, 10)  ==>  "5 is 50% of 10"//percentOf(2, 10)  ==>  "2 is 20% of 10"// Note: please write one or two lines here describing your solution.// first we need how much num2 per num1 equal then that result we calculated to see how much percent it isfunction percentOf(num1, num2) {    // TODO: your code here    var tmp = num2/num1;    var ans = 100/tmp;    return ans;}//=============================================== 2//Write a function called pluralize that://takes 2 parameters, a noun and a number.//returns the number with the noun in pluralized form.//pluralize('cat', 0)  ==>  "0 cats"//pluralize('cat', 5)  ==>  "5 cats" //pluralize('cat', 1)  ==>  "1 cat" // Note: please write one or two lines here describing your solution.// if there is only 1 cat we dont need 's' at the end of the word cause it's singular// otherwise we add it in any casefunction pluralize(noun, number) {     // TODO: your code here     if(number == 1){     	return number +" "+ noun;     } else{     	return number +" "+ noun + "s";     }}//=============================================== 3// write a function called addOne that takes an array of numbers as an input, // and returns a new array with all array elements incremented by one as an output// Note : solve this question using while loop // addOne( [1,2,3,4] ) ==> [2,3,4,5]// addOne( [3,6,9] ) ==> [4,7,10] // Note: please write one or two lines here describing your solution.// to add One to all elements of array we just need to iterate through all elments and add 1// while(i--) will count if i===0 as well cause it check if the condition is true or false first then decrease by 1function addOne(array) {    // TODO: your code here    var i = array.length-1;    while(i--){        array[i]++;    }    return array;}//=============================================== 4/*Write a function that uses console.log to give the following triangle:### #########################*/// Note: please write one or two lines here describing your solution.// to draw the triange we need 2 loops, 1 loop will iterate 7 times as 7 is the maximum will name the index of iteratoin i// then we need another iterator that will iterate i times and construct the string with # each iteration then just print strfunction drawTriangle() {	// TODO: your code here	var max = 7;	for(var i=0; i<max; i++){		var str = "";		for(var j=0; j<=i; j++){			str = str + '#';		}		console.log(str);	}}//=============================================== 5//Using recursion, Write a function that accepts a string and returns the number of vowels in that string.//Note:Five of the 26 alphabet letters are vowels: A, E, I, O, and U.//countVowels("four score and seven years") ==> 9;//countVowels("rbk") ==> 0 function containsVowel(str, c){	if(str.length === 0){		return 0;	}	if(str[0] === c) return 1		else return containsVowel(str.slice(1), c);}function countVowels(string){    // TODO: your code here    //console.log(string);    var vowels = "aeiouAEIOU";    if(string.length === 0) return 0;    return containsVowel(vowels, string[0]) + countVowels(string.slice(1));}  //Good Luck :))